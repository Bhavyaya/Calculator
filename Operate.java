package mooc.vandy.java4android.calculator.logic;

public class Operate {
    /*
    A base class that has a unifying method operate() that all operation classes would call
    It operates on the two operands and the type of operation input by the user
     */
    public static String operate(){

        //The return statement would be updated in the appropriate function calls
        return("");
    }
}
